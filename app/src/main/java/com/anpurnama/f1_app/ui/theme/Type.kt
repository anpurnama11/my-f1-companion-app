package com.anpurnama.f1_app.ui.theme

import androidx.compose.material3.Typography

// M3 default typography — system font (Roboto) at standard Material sizes (12–34sp).
// The design spec's scale matches M3 defaults, so no per-role overrides are needed;
// add one here only when a screen needs a size M3 doesn't provide.
val Typography = Typography()
